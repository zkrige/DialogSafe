"""
Package initializer for the profanity remover pipeline.

Run the CLI via:

    python -m src.main --input movie.mp4 --output out/safe.mp4 --mode mute

"""

# Repo-local version string (no packaging metadata is currently used).
__version__ = "0.0.1"
