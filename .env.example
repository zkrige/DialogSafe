# Copy to .env and edit values
## =============================================
## Profanity Remover — Environment Configuration
## =============================================
##
## Only variables actually read by the application (or its bundled backends)
## are listed here.
##
## Notes:
## - Secrets should be left empty and provided securely.
## - CLI args take precedence over env, which take precedence over hardcoded defaults.
## - Empty/whitespace values are treated as unset.


## ----------------------
## Transcription backend
## ----------------------
## Select which speech-to-text backend to use.
## Valid values:
## - local_whisper  : run Whisper locally
## - openai_api     : use OpenAI Whisper via API (requires OPENAI_API_KEY)
WHISPER_BACKEND=local_whisper

## Whisper model name.
## - For WHISPER_BACKEND=local_whisper: tiny, base, small, medium, large (etc)
## - For WHISPER_BACKEND=openai_api    : whisper-1 (or other OpenAI transcription model name)
## Note: if you keep the default "base" while using openai_api, the app will
## automatically translate that to "whisper-1".
WHISPER_MODEL=base


## ----------------------------
## Main CLI options (env-aware)
## ----------------------------
## These map 1:1 to CLI flags (e.g. MODE -> --mode). If you pass the CLI flag,
## it overrides the env var.

## Censorship behavior.
## Valid values:
## - mute  : silence profanity spans
## - bleep : overlay a synthetic beep tone on profanity spans
MODE=mute

## Primary spoken language code used for transcription.
## Example values: en, es, fr (ISO 639-1).
AUDIO_LANGUAGE=en

## Audio chunk size for transcription (seconds).
CHUNK_LENGTH_SECONDS=300

## Minimum confidence threshold for profanity hits (0.0–1.0).
MIN_CONFIDENCE=0.6

## Merge adjacent profanity hits into a single span when the gap between them
## is <= this value (milliseconds).
MAX_GAP_COMBINE_MS=500

## Boolean values accept: true/false, 1/0, yes/no, on/off

## When true, write a profanity-masked transcript to OUTPUT_DIR/transcript_clean.txt.
EMIT_CLEAN_TRANSCRIPT=false

## When true, write subtitles (SRT) marking censored segments to OUTPUT_DIR/censored_subtitles.srt.
EMIT_SUBTITLES=false

## When true, enable verbose (debug) logging.
VERBOSE=false

## When true, copy extracted audio.wav and all chunk_XXX.wav files into
## OUTPUT_DIR/audio_debug for inspection.
DEBUG_DUMP_AUDIO=false

## Optional paths

## Directory for logs and transcripts (created if missing).
OUTPUT_DIR=out

## Optional profanity list path.
## If empty, the default list is used (prefers config/profanity_en.txt).
## Formats supported:
## - JSON: a list of strings (words/phrases)
## - Text: one term per line; blank lines and lines starting with # are ignored
PROFANITY_LIST=

## Optional WAV beep file path.
## Note: currently unused; bleep mode uses a synthetic tone instead.
BLEEP_SOUND_PATH=


## -----------
## OpenAI API
## -----------
## Only used when WHISPER_BACKEND=openai_api.
## No default; leave empty in templates.
OPENAI_API_KEY=
